{% extends 'partials/base.html.twig' %}

{% block content %}
    {% if page.header.space_slug %}
       {% set flex = grav.get('flex_objects') %}
       {% set space_dir = flex ? flex.getDirectory('social-spaces') : null %}
       {% set space = space_dir ? space_dir.getObject(page.header.space_slug) : null %}
       {% if space %}
       <div class="wiki-context-banner" style="background: var(--skn-bg-card, #1a1a2e); padding: 1rem; border-left: 5px solid var(--skn-primary, #6366f1); margin-bottom: 2rem; border-radius: 8px;">
           <p>Ce document appartient à l'espace : <a href="/room/{{ space.slug }}"><strong>{{ space.name }}</strong></a></p>
       </div>
       {% endif %}
    {% endif %}

    {{ page.content|raw }}
{% endblock %}
